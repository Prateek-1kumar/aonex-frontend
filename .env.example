# Frontend env. NEXT_PUBLIC_* values are inlined into the client bundle.

NEXT_PUBLIC_API_URL=http://localhost:8787

# The Nango public key (NOT the secret). Used by @nangohq/frontend
# to open the Connect UI. Find it in your Nango dashboard.
NEXT_PUBLIC_NANGO_PUBLIC_KEY=
