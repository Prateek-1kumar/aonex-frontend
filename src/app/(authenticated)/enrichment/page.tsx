import LockedSection from "@/components/locked-section";

export default function EnrichmentPage() {
  return (
    <LockedSection
      title="Enrichment"
      subtitle="Attribute Enhancement"
      message="Activates once your Catalog has at least one approved product version."
    />
  );
}
