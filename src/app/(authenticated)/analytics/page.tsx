import LockedSection from "@/components/locked-section";

export default function AnalyticsPage() {
  return (
    <LockedSection
      title="Analytics"
      subtitle="Performance Metrics"
      message="Available once your first channel sync attempt completes successfully."
    />
  );
}
