import LockedSection from "@/components/locked-section";

export default function CatalogPage() {
  return (
    <LockedSection
      title="Catalog"
      subtitle="Product Intelligence"
      message="Populates automatically after your first ingestion is approved by the policy engine."
    />
  );
}
