import LockedSection from "@/components/locked-section";

export default function OptimisationPage() {
  return (
    <LockedSection
      title="Optimisation"
      subtitle="Opportunity Engine"
      message="Requires at least 7 days of Analytics data to generate opportunity scores."
    />
  );
}
